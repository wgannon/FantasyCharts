import type { NextConfig } from "next";

const repo = "https://wgannon.github.io/FantasyCharts/";
const nextConfig: NextConfig = {
  output: "export",
  basePath: "/FantasyCharts",
  /* config options here */
};
module.exports = nextConfig;

